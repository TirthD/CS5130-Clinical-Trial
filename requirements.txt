# ---- Core ----
requests>=2.31.0          # HTTP client for API calls
pydantic>=2.5.0           # Data validation & response models
python-dotenv>=1.0.0      # Load .env variables

# ---- LLM / Agent ----
# openai>=1.12.0            # OpenAI API (or swap for your LLM provider)
# anthropic>=0.18.0       # Uncomment if using Claude as the LLM

# ---- Data Processing ----
geopy>=2.4.0              # Geocoding & distance calculation (city → lat/lon)
rapidfuzz>=3.6.0          # Fuzzy string matching for medical terms
cachetools>=5.3.0         # In-memory caching for API responses

# ---- Testing ----
pytest>=8.0.0             # Test framework
pytest-asyncio>=0.23.0    # Async test support
pytest-cov>=4.1.0         # Coverage reporting
responses>=0.25.0         # Mock HTTP requests in tests

# ---- Evaluation ----
scikit-learn>=1.4.0       # Metrics (precision, recall for eval)
pandas>=2.2.0             # Data analysis for evaluation results

# ---- Dev Tools ----
black>=24.0.0             # Code formatting
ruff>=0.2.0               # Linting
mypy>=1.8.0               # Type checking
jupyter>=1.0.0            # Notebooks for exploration

# ---- Optional: Rate Limiting ----
ratelimit>=2.2.1          # Decorator-based rate limiting